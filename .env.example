# 프론트는 same-origin으로 /api/v1/... 호출(Vite proxy → 백엔드 8081~8084).
# 다른 origin이 필요한 경우(예: 사내 dev에 배포된 백엔드 별도 호출)만 채운다.
# VITE_API_BASE_URL=

# 인증 우회 (개발 초기). 팀원이 OIDC PKCE 도입 시 false.
VITE_SKIP_AUTH=true
